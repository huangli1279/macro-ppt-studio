# ==================== Supabase 数据库配置 ====================
# 从 Supabase 项目设置中获取数据库连接字符串
# 获取步骤:
# 1. 登录 Supabase Dashboard (https://supabase.com/dashboard)
# 2. 选择你的项目
# 3. 进入 Settings -> Database
# 4. 找到 "Connection string" 部分
# 5. 复制 "Connection pooling" 选项下的连接字符串（推荐用于生产环境）
#
# 格式: postgres://postgres.[PROJECT_ID]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

DATABASE_URL=postgres://postgres.xxxxxxxxxxxx:your_password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# 注意事项:
# - 使用 Connection pooling 模式可以提供更好的性能
# - 如需直接连接（Direct connection），端口使用 5432 而不是 6543
# - 请确保密码不包含特殊字符，或正确进行 URL 编码


# 是否启用导出PDF功能 (true/false, 默认为 true)
NEXT_PUBLIC_ENABLE_PDF_EXPORT=true

# ==================== AI Agent 配置 ====================
# OpenAI API 配置 (必填)
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI Base URL (可选，用于兼容 API，默认使用 OpenAI 官方 API)
# 例如: https://api.openai.com/v1 或自托管 API 地址
OPENAI_BASE_URL=

# OpenAI 模型名称 (可选，默认为 gpt-4o-mini)
# 如果使用兼容 API，可能需要修改为对应的模型名称
OPENAI_MODEL=gpt-4o-mini

# Tavily Search API 密钥 (用于联网搜索功能)
# 获取地址: https://tavily.com/
TAVILY_API_KEY=tvly-your-api-key-here
