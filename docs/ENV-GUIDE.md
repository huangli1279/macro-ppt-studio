# 环境变量快速指南

## 📁 文件说明

- **`.env.example`** - 环境变量模板（提交到 Git）
- **`.env`** - 实际使用的配置文件（不提交到 Git）

## 🚀 快速开始

### 1. 复制模板

```bash
cp .env.example .env
```

### 2. 配置 Supabase 数据库

编辑 `.env` 文件，添加你的 Supabase 数据库连接字符串：

```env
DATABASE_URL=postgres://postgres.xxxxxxxxxxxx:your_password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

### 3. 获取 Supabase 连接字符串

1. 登录 [Supabase Dashboard](https://supabase.com/dashboard)
2. 选择你的项目（如果没有，创建一个新项目）
3. 进入 **Settings** -> **Database**
4. 找到 **Connection string** 部分
5. 选择 **Connection pooling** 标签
6. 复制连接字符串
7. 将 `[YOUR-PASSWORD]` 替换为你的实际密码

## 📝 环境变量说明

| 变量名 | 说明 | 是否必需 | 示例 |
|--------|------|---------|------|
| `DATABASE_URL` | Supabase PostgreSQL 连接字符串 | 是 | `postgres://postgres.xxx:pass@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres` |

## ✅ 测试配置

启动开发服务器：

```bash
npm run dev
```

如果看到控制台输出：
```
📊 Database: Supabase (PostgreSQL) connected
```

说明数据库连接成功！

## 🔐 安全注意事项

1. **永远不要提交 `.env` 文件到 Git**
   - `.env` 文件已加入 `.gitignore`
   - 包含敏感信息（数据库密码）

2. **使用强密码**
   - 为 Supabase 项目设置强密码
   - 避免使用默认密码

3. **团队协作**
   - 团队成员需要自行创建 `.env` 文件
   - 可以通过安全渠道共享连接字符串
   - 或者每个成员使用自己的 Supabase 项目

4. **生产环境**
   - 在部署平台（Vercel/Netlify）的环境变量设置中配置
   - 不要在代码中硬编码数据库连接信息

## 🌍 不同环境的配置

### 开发环境

在本地 `.env` 文件中配置：

```env
DATABASE_URL=postgres://postgres.dev-project:dev-password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

### 生产环境

在部署平台的环境变量中配置：

**Vercel:**
1. 进入项目设置
2. Environment Variables
3. 添加 `DATABASE_URL`

**Netlify:**
1. Site settings
2. Environment variables
3. 添加 `DATABASE_URL`

**VPS:**
在服务器上创建 `.env` 文件：
```bash
nano .env
# 添加 DATABASE_URL
```

## ❓ 故障排查

### 连接失败

如果遇到数据库连接错误：

1. **检查连接字符串格式**
   ```
   postgres://postgres.[PROJECT_ID]:[PASSWORD]@[HOST]:6543/postgres
   ```

2. **密码中的特殊字符**
   - 如果密码包含特殊字符（`@`, `#`, `%` 等），需要进行 URL 编码
   - 或者在 Supabase 中重置密码为不含特殊字符的密码

3. **项目状态**
   - 确认 Supabase 项目状态为 "Active"
   - 检查项目是否暂停（免费计划长期不用会暂停）

4. **网络连接**
   - 确认网络可以访问 Supabase 服务器
   - 检查防火墙设置

### 环境变量未加载

如果环境变量没有生效：

1. **检查文件名**
   - 确保文件名是 `.env`（不是 `.env.txt` 或其他）

2. **重启开发服务器**
   - 修改 `.env` 后需要重启服务器
   ```bash
   # Ctrl+C 停止服务器
   npm run dev
   ```

3. **检查 .env 位置**
   - `.env` 文件应该在项目根目录
   - 与 `package.json` 同级

## 📚 相关文档

- [Supabase 设置指南](./supabase-setup.md)
- [部署指南](./deployment.md)
- [Supabase 官方文档](https://supabase.com/docs)

## 🆘 需要帮助？

如果遇到问题：

1. 查看 [Supabase 设置指南](./supabase-setup.md)
2. 查看项目 [README.md](../README.md)
3. 在 GitHub Issues 中提问
