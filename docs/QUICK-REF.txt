┌─────────────────────────────────────────────────────────────┐
│                 环境变量配置快速参考                        │
└─────────────────────────────────────────────────────────────┘

📁 配置文件
  .env.example  → 模板文件（提交到 Git）
  .env          → 实际配置（不提交，自动忽略）

🚀 快速开始
  $ cp .env.example .env
  $ npm install
  $ npm run db:test

🔄 切换数据库
  
  开发环境（SQLite）:
  ┌─────────────────────────────────────┐
  │ DATABASE_TYPE=sqlite                │
  │ SQLITE_DB_PATH=./data/ppt.db        │
  └─────────────────────────────────────┘
  
  生产环境（MySQL）:
  ┌─────────────────────────────────────────────────────────┐
  │ DATABASE_TYPE=mysql                                     │
  │ MYSQL_URL=mysql://user:password@host:3306/database     │
  └─────────────────────────────────────────────────────────┘

🛠 常用命令
  npm run db:test      → 测试数据库连接
  npm run db:push      → 应用数据库变更
  npm run db:studio    → 打开数据库管理界面
  npm run db:migrate   → SQLite → MySQL 数据迁移
  npm run dev          → 启动开发服务器

📝 注意事项
  ✓ .env 已加入 .gitignore
  ✓ 不区分 .env.local 和 .env.production
  ✓ 统一使用 .env 文件
  ✓ 修改后重启应用生效

📚 详细文档
  docs/ENV-GUIDE.md        → 环境变量指南
  docs/database-config.md  → 数据库配置说明
  docs/deployment.md       → 生产环境部署
  docs/CHANGES.md          → 更新日志

